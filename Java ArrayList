1.USING NEW ARRAYLIST<>()

  if (new ArrayList<>(dq).equals(Arrays.asList(s1.toCharArray())))
    return true;
            

2. COMBINE 2 ARRAYLISTS
    list1.addAll(list2);

3.CONVERTING ARRAYLIST TO INT[] ARRAY
    int[] array = list.stream().mapToInt(Integer::intValue).toArray();


4.FIND THE DIFFERNCE BETWEEN 2 ARRAYLISTS
    ArrayList<Integer> list1 = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5));
    ArrayList<Integer> list2 = new ArrayList<>(Arrays.asList(3, 4, 6, 7));

    // Clone list1 to avoid modifying the original list
    ArrayList<Integer> difference = new ArrayList<>(list1);
    difference.removeAll(list2);
